#pragma once

namespace Tmk
{
    /// <summary>Contains the terminal font weights</summary>
    enum class FontWeight
    {
        /// <summary>Usually rendered with bold font weight and/or bright colors.</summary>
        Bold = 1,
        /// <summary>Usually rendered with faint colors.</summary>
        Dim
    };
}
