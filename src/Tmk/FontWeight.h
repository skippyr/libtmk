#pragma once

namespace Tmk
{
    /// <summary>Contains the terminal font weights</summary>
    enum class FontWeight
    {
        /// <summary>The bold weight: usually rendered with bold font weight and/or bright colors.</summary>
        Bold = 1,
        /// <summary>The dim weight: usually rendered with faint colors.</summary>
        Dim
    };
}
